import { AccountPage } from "@/components/secondary";
export const metadata = {
  title: "Minha participação | CORRIDA 26",
  robots: { index: false, follow: false },
};
export default function Page() {
  return <AccountPage />;
}
